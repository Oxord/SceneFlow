# General exclusions (OS, IDE settings)
.DS_Store
Thumbs.db
.vscode/                 # VS Code workspace settings
.idea/                   # IntelliJ/Rider IDE settings
*.swp
*.swo
*.user                   # Visual Studio user files (e.g., .csproj.user, .sln.user)
*.suo                    # Visual Studio solution user options
*.log                    # General log files (can be made more specific if needed)
*.docx                   # Project specific (from your original)
*.pdf                    # Project specific (from your original)

# Python Specific (for NN folder and general Python tools)
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class
# C extensions
*.so
# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
# PyInstaller
*.manifest
*.spec
# Installer logs
pip-log.txt
pip-delete-this-directory.txt
# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
# Translations
*.mo
*.pot
# Django specific (if not needed, можете удалить или сделать более специфичным для NN/)
local_settings.py
db.sqlite3
db.sqlite3-journal
# Flask specific (if not needed, можете удалить или сделать более специфичным для NN/)
instance/
.webassets-cache
# Scrapy specific (if not needed, можете удалить или сделать более специфичным для NN/)
.scrapy
# Sphinx documentation
docs/_build/
# PyBuilder
target/
# Jupyter Notebook
.ipynb_checkpoints
# IPython
profile_default/
ipython_config.py
# pyenv
.python-version
# pipenv
#Pipfile.lock # See comment in original, keep commented for now.
# PEP 582; used by e.g. github.com/David-OConnor/pyflow
__pypackages__/
# Celery stuff
celerybeat-schedule
celerybeat.pid
# SageMath parsed files
*.sage.py
# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/
# Spyder project settings
.spyderproject
.spyproject
# Rope project settings
.ropeproject
# mkdocs documentation
/site
# mypy
.mypy_cache/
.dmypy.json
dmypy.json
# Pyre type checker
.pyre/


# C# / .NET Specific (for API folder and general .NET tools)
# Visual Studio / IDE specific files
.vs/                      # Visual Studio solution settings for the *root* if it exists, but usually inside solution folder
*/.vs/                    # Visual Studio solution settings (will match API/.vs/ for example)
*.bak                     # Backup files
*.docstates               # Visual Studio designer files
*.opensdf                 # Visual Studio SDF files
*.sdf                     # Visual Studio SDF files
*.tlog                    # Temporary log files
*.vmd/                    # Visual Studio UML files
*.vspscc                  # Visual Studio Project SCC file
*.vssscc                  # Visual Studio Solution SCC file
# Build output
*/bin/                    # Compiled binaries
*/obj/                    # Intermediate build objects
*/publish/                # Output of dotnet publish
*/TestResults/            # Test results output
# NuGet packages
*/packages/               # NuGet packages restored to solution-level packages folder
*.nupkg                   # NuGet package files
# .NET-specific caches and configuration
*/UserSecrets/            # .NET User Secrets
*.csproj.user             # Visual Studio project user settings
*.resharper               # JetBrains ReSharper config
*.dotsettings.user        # JetBrains Rider/ReSharper user settings
*/riderModule.iml         # JetBrains Rider module file
*/_ReSharper.Caches/      # JetBrains ReSharper caches
# Other common .NET exclusions
*.trx                     # MSTest test results
*.cache                   # General cache files (already in Python, but good to be explicit for .NET too)
# ASP.NET Core-specific
appsettings.Development.json # Often contains local development settings that shouldn't be committed